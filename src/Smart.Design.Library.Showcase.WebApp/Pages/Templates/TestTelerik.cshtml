@page
@model Smart.Design.Library.Showcase.Pages.Templates.TestTelerikModel
@using Kendo.Mvc.UI

@{
    ViewData["Title"] = "Test for Telerik";
}

<header class="o-container-vertical c-navbar c-navbar--white c-navbar--entry">
    <div class="o-container o-container--small">
        <div class="o-grid">
            <div class="o-grid-col-6">
                <div class="c-brand c-brand--small">
                    <a href="/home?lang=FR">
                        <img src="~/images/logo.svg" alt="Smart">
                    </a>
                </div>
            </div>

            <div class="o-grid-col-6">
                <ul class="c-pill-navigation">
                    <li class="c-pill-navigation__item c-pill-navigation__item--has-child-menu u-spacer-left-auto">
                        <button type="button" data-menu="lang-switch" data-menu-samewidth="true">
                            Fran&#231;ais
                        </button>
                        <ul class="c-menu c-menu--large" id="lang-switch">
                            <li class="c-menu__item">
                                <p class="c-menu__info u-text-muted">Fran&#231;ais</p>
                            </li>
                            <li class="c-menu__item">
                                <a class="c-menu__label" href="?lang=NL">Nederlands</a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</header>

<main class="o-container-vertical">

    <div class="o-container o-container--small">

        <div class="c-panel u-shadow-50">

            <div class="c-panel__body u-padding-vertical-l u-padding-bottom">
                @using (Html.BeginForm())
                {
                    <div class="form-group">
                        <label for="Nom">Nom * :</label>
                        @(Html.Kendo().TextBoxFor(m => m.Model.Nom)
                            .HtmlAttributes(new { @class = "form-control" }))
                        <span class="text-danger">@(Html.ValidationMessageFor(m => m.Model.Nom))</span>
                    </div>

                    @*<div class="form-group">
                        <label for="DateNaissance">Date de naissance :</label>
                        @(Html.Kendo().DatePickerFor(m => m.Model.DateNaissance)
                            .HtmlAttributes(new { @class = "form-control" }))
                        <span class="text-danger">@(Html.ValidationMessageFor(m => m.Model.DateNaissance))</span>
                    </div>
                    
                    
                    <div class="form-group">
                        <label for="Nom">Nom * :</label>
                        @(Html.Kendo().TextBox()
                            .Name("Nom")
                            .Value(FieldsForm.Nom)
                            .HtmlAttributes(new { @class = "form-control " }))
                        <span class="text-danger">@(Html.ValidationMessageFor(m => m.Nom))</span>
                    </div>
                    
                    <div class="form-group">
                        <label for="Genre">Genre :</label>
                        @(Html.Kendo().DropDownListFor(m => m.Genre)
                            .DataTextField("Text")
                            .DataValueField("Value")
                            .Items(items =>
                            {
                                items.Add().Text("Homme").Value("Homme");
                                items.Add().Text("Femme").Value("Femme");
                                items.Add().Text("Autre").Value("Autre");
                            })                            
                            .HtmlAttributes(new { @class = "form-control" }))
                        <span class="text-danger">@Html.ValidationMessageFor(m => m.Genre)</span>
                    </div>

                    <div class="form-group">
                        <label for="DateNaissance">Date de Naissance :</label>
                        @(Html.Kendo().DatePicker()
                            .Name("DateNaissance")
                            .Min(new DateTime(1900, 1, 1)) // Sets the min date of the DatePicker.
                            .Max(new DateTime(2099, 12, 31)) // Sets the max date of the DatePicker.
                            .Value(DateTime.Today) // Sets the value of the DatePicker.
                            .HtmlAttributes(new { @class = "form-control" }))
                        <span class="text-danger">@Html.ValidationMessageFor(m => m.DateNaissance)</span>
                    </div>*@

                    @(Html.Kendo().Button()
                        .Name("kendoIconTextButton")
                        .Icon("k-i-save")
                        .Content("Envoyer")
                        .HtmlAttributes(new { @class = "c-button c-button--primary", type = "submit" })
                        )
                }
            </div>
        </div>
    </div>

    <div class="o-container o-container--small">

       @*  <div class="c-panel u-shadow-50">

            <div class="c-panel__body u-padding-vertical-l u-padding-bottom">
                @(Html.Kendo().Switch()
                    .Name("switch")
                    .HtmlAttributes(new { @class = "tl-custom-switch" })
                    .Messages(c => c.Checked("Active").Unchecked("Inactive"))
                    )
            </div>
         </div> *@
    </div>>
</main>
<style>
    .tl-custom-switch {
        width: 10rem;
    }
</style>

